<%= form_with(model: complete_puzzle, local: true) do |form| %>
  <% if complete_puzzle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complete_puzzle.errors.count, "error") %> prohibited this complete_puzzle from being saved:</h2>

      <ul>
      <% complete_puzzle.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :completePuzzle %>
    <%= form.number_field :completePuzzle %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
